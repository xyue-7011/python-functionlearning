'''编写一个将十进制转换为二进制的函数，要求采用“除2取余”的方式，结果与调用 bin() 一样返回字符串形式'''  
def Dec2Bin(dec):
    temp=[]
    result=''         #最后输出为字符串
    while dec:
        quo=dec//2    #除2的商
        dec=dec%2     #除2的余数,余数为0的时候结束循环
        temp.append(quo)
    while temp:
        result +=str(temp.pop())   
    return result
